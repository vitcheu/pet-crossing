spring:
  config:
    activate:
      on-profile:
      - test
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/prop_db?useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    enabled: false
